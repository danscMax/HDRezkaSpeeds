{
  "name": "speeds-feedback-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that forwards feedback from HDRezkaSpeeds and VideoSpeeds extensions to a Telegram bot.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@cloudflare/vitest-pool-workers": "^0.16.3",
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.0.0"
  }
}
