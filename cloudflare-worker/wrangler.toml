name = "speeds-feedback"
main = "src/index.ts"
compatibility_date = "2026-05-06"

# CORS Worker — no observability dashboard needed beyond Cloudflare's own tail.
workers_dev = true

[vars]
# Comma-separated list of `app` field values the Worker will accept.
# Add new entries here when a new sister extension hooks into the
# same feedback endpoint.
ALLOWED_APPS = "hdrezka,videospeeds"

# Comma-separated chrome-extension host ids to pin the Chrome origin to.
# Leave EMPTY until the Chrome Web Store id is known — empty accepts any
# chrome-extension:// origin (dev-unpacked builds get a different id each
# load). After CWS publish, set this to the published id to lock Chrome down.
# Firefox uses a random per-install moz-extension:// origin and cannot be
# pinned, so it is always allowed at the protocol level.
ALLOWED_CHROME_IDS = ""

# KV namespace for per-IP rate limiting. Create with:
#   wrangler kv namespace create RATE_LIMIT
# Then paste the returned `id` below in place of "REPLACE_WITH_KV_ID".
[[kv_namespaces]]
binding = "RATE_LIMIT"
id = "4c6a64b0790940f180c479dd2755fa3e"

# Secrets are NOT declared here — set them with:
#   wrangler secret put TELEGRAM_BOT_TOKEN
#   wrangler secret put TELEGRAM_CHAT_ID
